#include "game.h"

#include <iostream>
#include <limits>
#include <string>

Difficulty GameDiff;

int main() {
  
  //Game *game = new Game(name.c_str(), GameDiff);
  //game->run();
  //delete game;
  return 0;
}